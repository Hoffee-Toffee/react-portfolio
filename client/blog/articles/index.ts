import type { Article } from '../types.ts'
import choco from './choco.ts'

// Add new articles here — they appear in the blog index in order
const articles: Article[] = [choco]

export default articles
